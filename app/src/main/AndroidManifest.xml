<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Admin"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UploadBook" />
        <activity android:name=".UploadResult" />
        <activity android:name=".UploadAttendance" />
        <activity android:name=".UploadQuestionPapers" />
        <activity android:name=".UploadSyllabus" />
        <activity android:name=".Main.MainScreen">

        </activity>
        <activity android:name=".timetable.UploadTimetable" />
    </application>

</manifest>